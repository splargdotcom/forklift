# I Don't Give A Forklift

A browser forklift game published by Splarg on itch.io.

## Current source

The root `index.html` is **v1.40**, preserved from the current development/release source on 19 September 2026.

The repository also contains development material retained separately from the playable root build. Future changes should be committed after this release snapshot so the history remains recoverable.

## Run locally

Open `index.html` in a modern browser.

## Project

HTML5 / CSS / JavaScript.
